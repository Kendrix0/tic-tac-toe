const displayController = (() => {

})();

const gameBoard = (() => {

})();

const player = (name, sign) => {
    const getName = () => name;
    const getSign = () => sign;

    return {getName, getSign}
};